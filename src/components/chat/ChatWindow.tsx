// TODO: Render the chat window shell for the conversational commerce agent.
export interface ChatWindowProps {}

export function ChatWindow(_props: ChatWindowProps): React.ReactElement {
  return <section />;
}
